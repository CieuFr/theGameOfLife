package jeudelavie.model;

public class FigureModel extends CanvasModel { // TODO make CanvasModel not abstract and use it directly
    public FigureModel(int size) {
        super(size);
    }
}
